class SnakePart {
	field int xPos; // x position
	field int yPos; // y position
	
	field int length; // size of the snake part

	constructor SnakePart new(int nX, int nY, int nLength) {
		let xPos = nX;
		let yPos = nY;
		let length = nLength;
		
	}